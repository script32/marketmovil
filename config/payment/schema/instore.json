{
    "properties": {
        "orderStatus": {
            "type": "string",
            "enum": ["Completado", "Pagado", "Pendiente"]
        },
        "buttonText": {
            "type": "string"
        },
        "resultMessage": {
            "type": "string"
        }
    },
    "required": [
        "orderStatus",
        "buttonText",
        "resultMessage"
    ],
    "additionalProperties": false
}
